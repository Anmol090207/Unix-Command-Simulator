# Unix-Command-Simulator
A simple UNIX Command Simulator built in C for Windows, implementing basic shell commands like pwd, ls, mkdir, rm, and cd using Windows-specific APIs. This project demonstrates how low-level system calls and file/directory operations work behind common terminal commands.
